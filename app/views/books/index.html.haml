%h1 Books

%table.table.table-striped
  %tr
    %th Title
    %th Author
    %th Type
    %th Actions
  - @books.each do |book|
    %tr
      %td= book.title
      %td= book.author
      %td= book.book_type
      %td= link_to 'Show', book_path(book)

= link_to 'Back to list', root_path

