%h1 Books

%p= link_to 'New book', new_admin_book_path

%table.table.table-striped
  %tr
    %th Title
    %th Author
    %th Type
    %th Actions
  - @books.each do |book|
    %tr
      %td= book.title
      %td= book.author
      %td= book.book_type
      %td
        = link_to 'Show', admin_book_path(book)
        = link_to 'Edit', edit_admin_book_path(book)
        = link_to 'Delete', admin_book_path(book), method: :delete, data: {confirm: 'Are you sure?'}

